language: python
dist: xenial
python:
  - 2.7
  - 3.6
  - 3.7
cache: pip
before_install:
  - pip install codecov
install:
  - pip install -r requirements.txt
  - pip install .
script:
  - coverage run -m unittest discover
after_success:
  - codecov --token=$CODECOV_TOKEN